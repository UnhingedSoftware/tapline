[package]
name = "tapline-wire"
description = "The protobuf wire codec: varints, length-delimited fields, and the Message trait"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme.workspace = true
keywords.workspace = true
categories.workspace = true
rust-version.workspace = true

# A leaf. No dependencies is the point: this is what keeps `prost` out of every
# downstream consumer's graph.
[dependencies]

[lints]
workspace = true
