[package]
name = "tapline-ids"
description = "Steam identifier types: SteamID, AppID, DepotID, ManifestID, PublishedFileID, EResult"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

# A leaf: no dependencies at all, internal or external. It compiles the instant
# the build starts, which is the point of the wide DAG.
[dependencies]

[lints]
workspace = true
